# apiVersion: v1
# kind: Service
# metadata:
#   name: api-gateway
# spec:
#   type: LoadBalancer
#   selector:
#     app: api-gateway
#   ports:
#     - protocol: TCP
#       port: 80
#       targetPort: 8080

apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  selector:
    app: api-gateway
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
